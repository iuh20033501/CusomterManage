eureka:
  client:
    service-url:
      defaultZone: http://localhost:3000/eureka/
    register-with-eureka:
      value: true
    fetch-registry:
      value: true
    instance:
      hostname: localhost

